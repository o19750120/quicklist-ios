# 給 Mac session 的交接

**狀態：草稿累積中，還沒送出。**

Mac 目前用不了，所以 Windows 這邊的改動先累積在這裡，
等使用者說可以了再一次交接過去。在那之前這份文件會持續增補 ——
Windows 這邊每做一件 Mac 需要知道的事，就往下面的「變更紀錄」加一筆。

送出前要先做的事列在最後一節。

---

## 一、Mac 那邊還不知道的狀態

### 資料表只加了一個欄位，舊版 App 不受影響

`CLAUDE.md` 警告過「後端改動會遠端弄壞 App」，所以先講清楚：
只在 `kikitori_transcripts` **新增**了 `vocab jsonb`（可為 null），
沒有改也沒有刪任何既有欄位。已經裝在 iPad 上的版本照樣運作。

那個欄位是給「點詞查意思」用的，格式與用法見最下面變更紀錄第一條。

### 新增：離線日文字典（`backend/dictionary.py`）

使用者的要求是「直接塞一本日文字典在資料庫裡面，完全不經過任何模型處理」。
做出來了，**查詞零 API 呼叫**。

| 來源 | 內容 | 授權 |
|---|---|---|
| JMdict | 21.8 萬條目，英文釋義、讀音、詞性 | CC BY-SA 4.0 |
| JMnedict | 74.3 萬人名地名 | CC BY-SA 4.0 |
| Tomoshi | **21.7 萬條繁體中文釋義**（25.1 萬義項） | CC BY-SA 4.0 |

建成一個自包含的 SQLite，962,296 條目 / 1,369,667 個詞形 / **293 MB**。

```bash
python backend/dictionary.py --build            # 下載並建置（要 ~10 分鐘）
python backend/dictionary.py --lookup 市場 --reading シジョウ
python backend/dictionary.py --coverage         # 拿真實逐字稿量涵蓋率
```

**實測涵蓋率**（用我們自己的 podcast 逐字稿，不是通用語料）：
7,314 詞次中查得到 96.6%、有中文釋義 96.0%。
查不到的幾乎只剩數字與英文縮寫。

查詢介面：

```python
from dictionary import Dictionary
d = Dictionary()
d.lookup("市場", reading="シジョウ")
# → {"form","readings","senses"(英),"zh"(繁中列表),"common","source","matched_reading"}
```

### 中文釋義必須跟著讀音走（這是踩過的坑）

`lookup()` 的 `reading` 參數不是可有可無的。中文表原本只用詞形當鍵，
結果會出現「英文挑對了、中文卻是另一個意思」：

```
市場【シジョウ】→ 英文 financial market  ✓
              → 中文「(城鎮)市場；街市」 ✗  這是 いちば 的意思
```

畫面上看起來完全正常，所以特別危險。現在改成 (詞形, 讀音) 雙鍵：

```
市場【シジョウ】→ 市場（金融、股票）；交易所
市場【イチバ】 → (城鎮)市場；街市；集市
人気【ニンキ】 → 人氣；受歡迎程度
人気【ヒトケ】 → 人跡；有人的跡象
```

**讀音從哪來**：Sudachi 的 `reading_form()`，轉錄流程本來就在算，不必額外做事。
App 端如果要查詞，讀音要一起帶上，否則同形異讀的詞會給錯意思。

### 其他改動

- `.gitignore` 加了 `backend/data/`（293 MB 字典資料不進版控，`--build` 可重建，
  而且 repo 是 public）
- `TODO.md` 的「點單字查字典」整段改寫。原本記的方案是「每集用 LLM 預建詞表」，
  **使用者已經否決**（「字典應該是離線版本才對不會打 api 太浪費了」），不要再提。

---

## 二、Mac 要做的事

### 1. App 裡放出處標示 —— 這是法律義務，優先

CC BY-SA 4.0 要求標示出處。字串已經在 `backend/dictionary.py` 的
`ATTRIBUTION` 常數裡，照抄即可：

```
JMdict / JMnedict / KANJIDIC © Electronic Dictionary Research and
Development Group — CC BY-SA 4.0
中文釋義 © Y1Z (Tomoshi) — CC BY-SA 4.0
```

放在設定頁或關於頁都行，只要使用者找得到。

**授權的連鎖範圍要講清楚**：BY-SA 的 ShareAlike 只及於**資料**，
不及於 App 的程式碼。我們沒有修改字典資料的話沒有額外義務；
我們確實有修改（詞層級去重、陸→台用詞替換 3,080 處），
所以改後的**資料**要以 BY-SA 釋出 —— 但那是資料，Swift 程式碼不受影響。

另外不能用 "Tomoshi" 這個名稱或 logo 當我們的產品識別。

### 2. 查字典的 UI —— 資料全部到位了，只缺 Swift

架構已經定了（見下一節），`vocab` 欄位裡有該集的詞表與詞邊界。
要做的事：

1. 讀 `transcript.vocab`（可能是 null，舊資料沒有 → 就不顯示查詞功能）
2. 依 `tokens[句index]` 的 `[起, 迄, lemma]` 把句子切成可點的片段
3. 點下去用 lemma 去 `vocab` 取 `zh`（繁中）與 `en`（JMdict 英文）
4. 有 `alt` 的顯示「這個字還有其他讀法」；有 `guess` 的假名要標得保守

**不需要任何額外網路請求，也不需要在 App 裡放字典檔。**

### 3. 側邊小助手：兩種播放模式

使用者明確講過的設計，先做兩種、之後再擴充：

- **正常播放**（預設）
- **逐句重聽**：播完一句自動回到句首重播，直到手動往下

逐句重聽的技術基礎已經有了 —— 單擊某句會 Spotify seek 到那句開頭，
實測誤差 0.8 秒（點第 58 句 326.7 秒，Spotify 跳到 327.5 秒）。

### 4. 查不到的詞給外部連結

離線詞表涵蓋 96.6%，剩下的多是數字與英文縮寫。查不到時可以給
「在 Weblio 查」「在 Jisho 查」的外部連結。

**開連結沒有授權問題，抓資料才有。** Jisho 官方明確禁止爬取
（10 req/10 秒就 429）、goo辞書 2025-06 已停止服務、Weblio 與 kotobank
沒有 API。所以只做 `openURL`，不要在 App 裡打那些站的 API。
日中辭典可以用 `cjjc.weblio.jp`（160 萬語，簡體中文）。

---

## 三、需要一起決定的（Windows 這邊不會單方面拍板）

### 字典怎麼送到 App —— 已經做了，這裡記結論

選了「每集預先算好該集詞表」。另外兩條路的問題：後端查詢 API 要連網、
點一個詞就一次往返；App 下載精簡版還是有數十 MB 而且要自己處理版本。

預建詞表一集只有 87–371 KB，隨逐字稿一起下，離線可用、查詢零延遲。
代價是點到詞表外的詞就沒有 —— 但實測涵蓋率 96.6%，缺的多是數字與縮寫。

照 CLAUDE.md 的規矩只加不改不刪：`vocab` 欄位可為 null，舊 App 忽略即可。

**還沒決定的**：使用者點到查不到的詞時要怎麼辦。一個沒有授權問題的做法是
開外部連結（「在 Weblio 查」「在 Jisho 查」），那不是抓資料所以沒問題 ——
但線上辭典**不能爬**（Jisho 官方明確禁止、goo辞書 2025-06 已關閉、
Weblio 與 kotobank 沒有 API）。這個等 Mac 那邊看實際體驗再決定。

---

## 四、不會從程式碼看出來的注意事項

- **Tomoshi 是單人維護的新專案**（2026-08 首版、GitHub 1 star）。
  `--build` 會核對 sha256，但上游若消失就重建不了。
  已經把 `tomoshi.db` 留在 `backend/data/cache/`（不進版控），
  Mac 那邊建議也自己存一份。
- **不要自己做簡轉繁**。Tomoshi 同一個檔案裡有簡體表，但實測拿它
  OpenCC 轉會錯約 2,000 條（`s2twp` 一致率只有 99.2%），
  而且錯得很難發現：「考試合格；通過考試」會變成「透過考試」。
  一律用 `zh_defs_zhtw`。
- **JMnedict 的同形異讀沒有頻率標記**。不帶讀音去查「名古屋」
  會拿到「たなごや」（某個罕見姓氏），不是名古屋市。

---

## 五、送出前要做的事

- [x] `python scripts/check.py` 通過
- [x] `backend/data/` 沒進版控（`.gitignore` 有擋，293 MB 且 repo 是 public）
- [x] commit + push（使用者已授權，之後不必再問）
- [ ] **Mac 開機時用 SendMessage 通知對方**，重點是：
      資料表多了 `vocab` 欄位（只加不刪）、兩個 UI 規格已定案、
      以及「不要裝 opencc-python-reimplemented」那條

---

## 變更紀錄

往下加，最新的在最上面。每加一項就寫清楚 Mac 那邊需不需要跟著動。

### 2026-09-06 後端 API 呼叫全面觀測 + 關思考的寫法逐一實測
**Mac 不用跟著動，但如果要碰 `backend/providers.py` 要知道這些。**

**每次 API 呼叫都有紀錄**（`providers.CALLS` / `call_summary()`）：
哪把金鑰（只記索引）、哪個模型、成功與否、耗時、錯誤是什麼。
轉錄跑完自動印摘要。加這個是因為犯過一次錯 —— 判斷「撞到配額」
但那是推論，查完整日誌才知道真正原因是涵蓋檢查沒過。

**關閉思考模式的寫法是分世代的，不能寫死一種**：

| | 寫法 |
|---|---|
| Gemini 3.6 / 3.5 / flash-lite | `thinkingLevel: MINIMAL` |
| Gemini 3.8 / 3.7 | `thinkingLevel: LOW`（沒有 minimal） |
| Gemini 2.5 | `thinkingBudget: 0`（3.x 傳這個有的會 400） |
| Groq qwen 系列 | `reasoning_effort: "none"` |
| Groq gpt-oss 系列 | `reasoning_effort: "low"`，**關不掉**，最低就是這樣 |

寫死一種的後果實測過：`gemini-3.6-flash` 一律 400、
`qwen3.6` 與 `groq/compound` 永遠失敗。現在改成依序試候選、
成功就記住，並用 `thoughtsTokenCount` / `reasoning_tokens`
**實際驗證思考真的是 0**，不是就印警告。

**其他後端改動**：靜音處的幻覺偵測（ffmpeg silencedetect）、
`language_codes` 維持 `["ja-JP","en-US"]`（加中文實測會讓日文被丟掉）。

### 2026-09-06 兩個 UI 決定已定案，規格如下（都是 Swift 工作）

使用者授權照建議做，不用再問他。

#### 一、說話者呈現

實際資料直接否決了最直覺的做法：

| 集數 | 說話者 | 切換頻率 |
|---|---|---|
| 大人の日本語 | 1 位 | 0 次／120 句 |
| ニュース小話 #230 | 2 位 | **236 次／674 句** |
| EP45 日本人森 | 2 位 | 40 次／133 句 |

**平均每 3 句換一次人**，所以背景色分色會讓畫面一直閃，
而且會跟「目前這句」的高亮打架 —— 那是這個 App 最重要的視覺訊號。

1. **只有一位說話者時完全不顯示**（`lines` 裡 `speaker` 只有一種值就關掉）。
   獨白節目佔多數，為不存在的區別加 UI 是純雜訊。
2. **多人時用前緣細線**（2–3pt，畫在句子左側，顏色取 Theme 的中性色階），
   **不要用背景色**。細線不影響日文可讀性，背景色會。
3. **只在「換人」那一句畫線**，連續同一人不重複畫。
   跟聊天介面把連續訊息併成一組同樣道理。
4. **名字讓使用者自己標**：長按 →「這是誰在說話」→ 輸入 →
   套用到該集所有同編號的句子，存 UserDefaults（跟「聽到哪」同性質）。
   沒標就只有顏色，一樣分得出誰是誰。
5. **不要做頭像或大標題。** diarization 會出錯，細線的視覺份量剛好對應
   它的可信度；頭像加名字會讓錯誤看起來像事實。

#### 二、假名不確定時顯示候選

後端已經算好了，App 只要照著顯示：

- `tokens` 的 span 第四個元素若是 **`"?"`** → **這個位置有兩種讀法**，
  顯示候選（候選清單在 `vocab[lemma].alt`），不要默默挑一個
- 第四個元素是**其他字串** → 那是覆核後的正確讀音，用它覆蓋 `vocab[lemma].r`
- 沒有第四個元素 → 用 `vocab[lemma].r`

**不要拿 `alt` 當「不確定」的判斷依據。** `alt` 只代表這個詞形在字典裡
有別的讀音，306 個詞裡有 110 個（36%）會中 —— 照那樣做三分之一的詞
都掛著候選，變成雜訊。真正的不確定是後端兩個模型意見不同，
實測只有 4%（139 個疑慮詞裡 6 個），而且都是真歧義：
皆（みな／みんな）、街中（まちなか／まちじゅう）、避け（さける／よける）。

### 2026-09-06 資料表新增 `vocab` 欄位 —— Swift 那邊要用到
**這是這批改動裡唯一影響 App 的。只加不刪，舊版 App 不受影響。**

`kikitori_transcripts` 加了 `vocab jsonb`（可為 null，舊資料沒有）。
內容是該集的詞表與詞邊界，轉錄時就建好，跟逐字稿一起下載，一集 87–371 KB。

```json
{
  "vocab": {
    "行く": {"r": "いく", "p": "動詞",
             "zh": ["去；移向；前往", "透過；橫貫", ...],
             "en": ["to go", "to move (towards)", ...],
             "alt": ["ゆく"],        // 多讀音時才有，給「這裡有兩種讀法」用
             "guess": true}         // 讀音是猜的，標假名要保守（很少見）
  },
  "tokens": [ [[0,1,"皆"], [1,3,"さん"], [3,4,null], ...], ... ]
}
```

**`tokens` 是重點，不是附加品。** 每個元素對應 `lines` 的同一句，
內容是 `[起始字元, 結束字元, 原形或 null]`。日文沒有空格，
**App 沒有這個就無法判斷使用者點到哪個詞**。null 代表助詞、標點這類
不用查的部分，但一樣要拿到才能正確畫出詞的邊界。

點詞查意思的流程因此是：用點擊位置在 `tokens` 裡找到那一段 →
拿 lemma 去 `vocab` 取釋義。**不需要任何額外請求，也不需要字典檔。**

完整字典（293 MB）留在 runner 上，不進 App 也不進 Supabase。

### 2026-09-06 中文釋義品質全面量測（數字比先前回報的大兩個數量級）
先前回報「上游瑕疵只有 23 筆」是抽樣猜出來的，**低估很多**。系統性掃描後：

| 問題 | 全庫 | 常用詞 |
|---|---|---|
| 簡繁字形不一致 | 315（0.13%） | — |
| 有歧義的詞彙替換開火 | 1,729 | 213（0.71%） |
| 替換造成重複義項 | 1,831 | 904（3.0%）**已修** |
| 空釋義 / 義項數不符 | 20 | — |
| 翻譯本身（人工抽驗 24 常用詞） | 22/24 正確 | — |

- **已修**：重複義項在 `_import_chinese` 用**詞層級**去重（不是字串層級 ——
  單一個 gloss 自己就帶分號）。1,831 → 1,184，剩下的是跨義項的正常重複。
- **不修**：詞彙替換套錯語境（「簽署文件」→「簽署檔案」）。
  規則沒錯，錯在不看語境；`--audit` 會列出來，但要人判斷。
- **影響有限**：錯誤集中在科技詞彙（文件、程序、通信、对象），
  日常對話幾乎不出現，對這個 App 的實際影響小。

### 2026-09-06 中文釋義品質：pip 套件裝錯會得到完全不同的結果
**Mac 那邊如果要跑 `backend/dictionary.py`，這件事一定要知道。**

pip 上有兩個 opencc，行為差很多：

| 套件 | 「岩手县」 | 「通过考试」 | 「血症」 |
|---|---|---|---|
| `opencc`（官方 1.4.2） | 岩手縣 ✓ | 通過考試 ✓ | 血症 ✓ |
| `opencc-python-reimplemented` | 巖手縣 ✗ | 透過考試 ✗ | 血癥 ✗ |

後者內建很舊的字典快照。裝錯的話稽核結果會完全不同。

新增 `python backend/dictionary.py --audit`：拿 Tomoshi 的簡體表用官方
OpenCC 重轉，跟繁體表比對找出候選。實測 315 筆 / 25.1 萬（0.13%），
自動排除日文原文的偽陽性。**只報告不修改** —— 因為不一致不等於錯，
「幼稚園→幼兒園」是官方轉錯、「託兒所→托兒所」是 Tomoshi 錯，兩邊都有。

另外確認：**這份繁體釋義不會有香港用語**。OpenCC 的 HKVariants/HKPhrases
不在 s2tw/s2twp 鏈路裡。沒轉到的是大陸用語（軟件、網絡這種港陸共用詞）。

### 2026-09-06 後端硬編碼稽核（使用者要求）
使用者問「後端是否有任何寫死的設計」，掃過整個 `backend/`，改了三處：

- **拿掉 `dictionary._ZHTW_FIXES`**（9 條字串替換的個案補丁）。
  那張表是抽樣猜出來的，不是量出來的，修完會顯示「乾淨」但真實殘留量
  無從得知 —— 製造已修好的錯覺比誠實承認上游有瑕疵更糟。
  也確認沒有規則性解法（`t2tw` 只做字形標準化，修不掉選字錯誤）。
- **轉錄模型改成動態解析**（`providers.transcribe_model()`）。
  原本 `GEMINI_TRANSCRIBE_MODEL` / `WHISPER_MODEL` 是寫死的，
  跟當初讓翻譯靜默斷掉的 `llama-3.3-70b-versatile` 是同一個坑，只補了一半。
  現在改成「驗證過的候選清單 + 執行時確認還活著」，
  **全部不在就明確報錯換下一家，不會靜默降級成品質未知的模型**。
  Deepgram 沒有列模型的 API，仍寫死，但下架時會 400 所以不會靜默。
- **閾值補上實測依據**。順便量出一件事：`segment.py` 真正生效的限制是
  `MAX_CHARS=48` 不是 `MAX_SECONDS=10`（語速中位 6.9 字/秒，
  48 字約 7 秒就先觸發，只有 0.4% 的句子會碰到 10 秒）。
  以後想讓字幕短一點要調字數，調秒數沒有感覺。

**Mac 不用跟著動**：沒有動資料表，`source_model` 的格式也沒變
（一直都是 `gemini:gemini-3.5-transcribe` 這種形式）。

### 2026-09-06 離線日文字典完成
- 新增 `backend/dictionary.py`，96 萬條目 / 137 萬詞形 / 293 MB SQLite
- 中文釋義來源定案：Tomoshi（CC BY-SA 4.0，繁體，21.7 萬條）
- 改寫 `TODO.md` 的字典段落，移除已被否決的「LLM 預建詞表」方案
- `.gitignore` 加 `backend/data/`
- **Mac 要跟著動**：出處標示（法律義務）、查字典 UI（等架構決定）
- **不影響現有 App**：沒有動任何資料表或 API 格式
