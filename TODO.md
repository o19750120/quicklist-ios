# Kikitori 開發清單

聽 Spotify 上的 podcast 學語言：逐句顯示原文與翻譯，跟著播放進度走。
主力語言日文，但架構做成語言無關，之後能加其他語言。

**開發環境**：Windows 寫程式 → GitHub Actions 雲端 macOS 編譯 → iloader 匯入 iPad。
因為每次上機測試都得接電腦，所以盡量累積到一整塊功能可測了再匯入。

---

## 階段 0：建置管線 ✅ 已完成

- [x] XcodeGen + GitHub Actions 無簽名編譯，自動打包 `.ipa` 發到 Releases
- [x] Windows 端 Apple USB 驅動（Store 版 iTunes 缺驅動，改從安裝包解出 MSI 單獨安裝）
- [x] iloader 安裝，走通「匯入 IPA → iPad 上跑起來」
- [x] 建置結果推播到 Discord（成功給下載連結，失敗給 error 摘要）
- [x] `scripts/check.py` push 前本機驗證（YAML / plist / 資源檔 / Swift 括號）
- [x] `scripts/ship.ps1`、`scripts/get-ipa.ps1` 一鍵送出與取件

## 階段 1：Spotify 連動 ✅ 已完成

- [x] PKCE 授權流程（手機端不需要 client secret）
- [x] Client ID 由 GitHub Secret 在建置時注入，不進公開 repo
- [x] `kikitori://spotify-callback` URL scheme
- [x] 讀 `/me/player`，帶 `additional_types=episode` 才拿得到 podcast 資訊
- [x] 每 5 秒對一次 API、每 0.2 秒本地推算進度，畫面秒數平順前進
- [x] 登入、正在播什麼、設定三個畫面
- [x] **驗證 OAuth 能跳回 App**（iPad 實機與 Mac 模擬器都通過）
- [x] 診斷畫面：token 狀態、播放狀態、任務狀態、App 內執行紀錄（可一鍵複製）
      （讓一次匯入能回報最多資訊，減少來回次數）

## 階段 2：逐字稿產生管線（後端）✅ 本機與雲端都跑通

- [x] 用 iTunes Search API 從「節目名」找到公開 RSS
- [x] 從 RSS 比對出「集數名」對應的那一集，拿到音檔網址（四個真實日文節目全部命中）
      （名稱不會完全一樣，需要模糊比對；先在 Windows 本機用真實日文節目驗證）
- [x] Supabase 建表（`kikitori_` 前綴，放在 trendrace，未動既有的 `daily`）
      - `kikitori_episodes`：節目名、集名、Spotify episode id、RSS 音檔網址、時長
      - `kikitori_transcripts`：逐句時間軸、原文、翻譯
      - `kikitori_jobs`：轉錄任務狀態
- [x] GitHub Actions 轉錄工作流（Ubuntu runner）
      - [x] 下載音檔
      - [x] ffmpeg 壓成 16kHz 單聲道並依實際長度切段
      - [x] Groq Whisper 轉錄，逐段累加時間軸偏移
      - [x] 分批翻譯，強制輸出等長陣列確保逐句對齊
      - [x] 寫回 Supabase
- [x] 金鑰改用 `C:\Me\YT起號` 既有的（Deepgram / Groq×6 / Gemini×11 / AssemblyAI），全部驗證可用
- [x] 轉錄改用 Deepgram 直讀音檔網址：11 分鐘節目 4 秒轉完，省掉下載與 ffmpeg
- [x] 重寫斷句演算法：最長 51 秒 → 16.5 秒，平均 3.4 秒 22 字
- [x] 雲端實跑驗證：122 句已寫進 Supabase

## 階段 3：App 端逐字稿 ✅ 已完成

- [x] 從 Supabase 讀逐字稿（anon key，查詢格式已用測試資料驗證通過）
- [x] 逐句顯示，目前這句高亮，自動捲動（手動捲動時暫停跟隨 6 秒）
- [x] 中日對照，工具列可切換
- [x] **長按某句「現在講的是這句」重新對齊**，單擊則跳到該句（Spotify seek）
- [x] 沒有逐字稿時顯示「產生逐字稿」，排隊中每 20 秒自動查一次，好了自動出現

## 階段 3.5：書庫 ✅ 已完成

- [x] 聽過的節目與集數列表，依節目分組（`LibraryView`）
- [x] 每一集顯示：句數、上次聽到哪、完成度
- [x] 點進去可以純閱讀，不必正在播放也能看逐字稿（`ReaderView`），
      並停在上次聽到的那句
- [x] 播放進度記錄（`LibraryStore`，存 UserDefaults，之後可考慮同步）
- [x] 書庫點一集就叫 Spotify 播它，並從上次聽到的地方接下去，不必離開 App
      （已聽完的那幾集從頭開始；長按才是純閱讀。
      Spotify 完全沒有裝置時才退回用 URL scheme 把 Spotify 打開）

## 階段 6：轉錄品質工程（核心，優先於階段 4）

這是這支 App 的命脈 —— 逐字稿不準，後面的翻譯、假名、生字本全部沒有意義。

### Gemini 轉錄的配額與成本（實測）

- 免費層可用。配額是會回填的水桶（`generate_content_free_tier_input_token_count`
  limit 10000），桶滿時單發不預檢大小，97,516 tokens 的請求也放行，
  但打完就欠著，之後被擋 7–40 秒。
- 實務節奏約每把金鑰每分鐘 2 集（10 分鐘節目），8 把金鑰約 16 集/分鐘，夠用。
- 撞到回 HTTP 429、body 帶 `too_many_requests`（與翻譯那邊的配額錯誤文字不同），
  已接進換金鑰邏輯。
- 音訊固定 25 tokens/秒，可精準預估用量不必試打。
- 付費層 $0.005/分，一集約 $0.05，跟 Deepgram 的 $0.043 同級。

### 每一次 API 呼叫都有紀錄（`providers.CALLS` / `call_summary()`）

加這個是因為犯過一次錯：一集轉錄退回 Deepgram，我判斷是「撞到配額」，
但那是**推論不是證據** —— 查完整日誌才發現真正的原因是涵蓋檢查沒過
（Gemini 中間空白 29%）。而且我自己在指令加了 `| tail -35`，
把日誌開頭截掉，等於親手毀掉證據。

現在每次呼叫都記：哪一把金鑰（只記索引）、哪個模型、成功與否、
花多久、錯誤是什麼。轉錄跑完會印出摘要。

#### 實測：配額幾乎不是問題，真正的問題是別的

跑一集的完整紀錄（20 次呼叫）：

    gemini/gemini-3.5-transcribe    1 成功 0 失敗，金鑰 [0]，27 秒
    gemini/gemini-3.8-flash         0 成功 1 失敗 ← HTTP 503
    gemini/gemini-3.7-flash         0 成功 1 失敗 ← HTTP 503
    gemini/gemini-3.6-flash         0 成功 1 失敗 ← HTTP 400
    gemini/gemini-3.5-flash         1 成功 0 失敗，金鑰 [1]，18 秒
    gemini(讀音)/3.1-flash-lite      3 成功 0 失敗
    gemini(讀音)/2.5-flash-lite      3 成功 9 失敗 ← HTTP 404

**一次 429 都沒有。** 逐把金鑰 × 逐個模型掃過一遍（8 keys × 7 models）：

| 模型 | 可用的金鑰數 |
|---|---|
| gemini-3.5-flash | 8/8 |
| gemini-3.1-flash-lite | 8/8 |
| gemini-3.8-flash | 7/8（一把 503，暫時性） |
| gemini-3.7-flash | 7/8（一把 503，暫時性） |
| gemini-2.5-flash | 4/8（三把 404） |
| gemini-2.5-flash-lite | 3/8（四把 404、一把 429） |
| **gemini-3.6-flash** | **0/8 —— 全部 400** |

三件事：

1. **配額 429 只出現一次**（八把金鑰 × 七個模型 = 56 次呼叫裡一次）。
   「很容易撞到配額」是錯的判斷。
2. **不同金鑰能用的模型不一樣**。2.5 系列在四把金鑰上是 404，
   3.x 系列則幾乎都能用。所以「每把 key 能用很多模型」大致成立，
   但不是每把都一樣。
3. **`gemini-3.6-flash` 對全部八把金鑰都 400** —— 那不是配額也不是金鑰問題。
   逐項拆解後找到原因：**它不接受 `thinkingConfig.thinkingBudget=0`**，
   拿掉就成功。而 `gemini-3.8-flash` 需要那個設定（不關會花 387 tokens
   思考才產出 36 tokens）。所以不能二選一。

   處置：`_gemini_batch` 照預設送，撞到 400 就拿掉思考設定重試一次
   並記住這個模型（`_NO_THINKING_CONFIG`）。**不寫死模型名稱** ——
   關思考的語法每一代都在變（Gemini 3 已經不吃 `thinkingLevel: MINIMAL`），
   寫死的清單一定會過期。

#### 覆核與轉錄共用配額

`vocab.verify_readings` 倒著取金鑰，跟轉錄從相反方向消耗。
這是**預防不是修 bug** —— 上面的資料顯示配額目前不緊張。

### 金鑰使用策略

- [x] 一把金鑰要把它所有能用的模型額度都耗盡，才換下一把
- [x] 禁止同一把金鑰並行跑多個任務（同一時間只有一個請求）
- [x] **關閉思考模式 —— 逐一模型實測過**（2026-09-06 重做）。
      原本只驗過一兩個模型就把寫法寫死，結果**有模型因此永遠失敗、
      也有模型默默開著思考跑**。

      判斷標準是 `usageMetadata.thoughtsTokenCount`（Gemini）與
      `usage.completion_tokens_details.reasoning_tokens`（Groq）——
      「請求沒報錯」不等於「思考關掉了」，這兩件事要分開驗。

      **Gemini：兩種語法互補，不是二選一**

      | 模型 | `thinkingBudget:0` | `thinkingLevel:MINIMAL` |
      |---|---|---|
      | gemini-3.7-flash | 關掉 | 400 |
      | gemini-3.5-flash | 關掉 | 關掉 |
      | gemini-2.5-flash | 關掉 | 400 |
      | **gemini-3.6-flash** | **400** | **關掉** |
      | gemini-3.1-flash-lite | 關掉（本來就不思考） | 關掉 |

      寫死 `thinkingBudget` 會讓 3.6 直接 400；只是「撞到 400 就拿掉設定」
      的話它會**開著思考跑 799 個 token**，正好違背要求。

      **Groq：寫死 `reasoning_effort:"low"` 讓兩個模型永遠失敗**

      | 模型 | 不送參數 | `"low"` |
      |---|---|---|
      | openai/gpt-oss-120b | 279 | 25 |
      | openai/gpt-oss-20b | 327 | 9 |
      | qwen/qwen3.6-27b | **1303** | **400** |
      | groq/compound | 沒有推理 | **400** |

      `"none"` / `"default"` / `"minimal"` 全部 400，Groq 只認 low/medium/high。
      qwen3.6 不送參數會燒 1303 個推理 token，但送 low 又直接失敗 ——
      這種關不掉的情況要**講出來**，不要靜默。

      **官方文件補上了實測沒涵蓋的部分**（查證過，非臆測）：

      - **Gemini 3 世代改用 `thinkingLevel`（字串），2.5 世代才用
        `thinkingBudget`（數字），而且兩個不能同時傳，會 400。**
      - 各模型能接受的最低等級不同：**3.6／3.5／flash-lite 有 `minimal`，
        3.8／3.7 最低只到 `low`**。所以候選順序是 minimal → low → budget=0。
      - `gemini-2.5-pro` 的 budget 最小值是 128，**關不掉**；
        `gemini-2.5-flash-lite` 預設就是關的。
      - `gemini-3.5-transcribe` **沒有思考模式的概念**，`transcription_config`
        底下只有 `language_codes` / `custom_vocabulary` / `mode`。不要傳。
      - Groq：**qwen 系列吃 `"none"`（qwen3.8 預設就是 none），
        gpt-oss 只吃 low/medium/high、最低 `low`、關不掉。**
        `reasoning_format:"hidden"` 只是不回傳推理內容，**token 照樣算錢**。
        `/no_think` 是 Qwen 自架部署的慣例，Groq 官方文件沒有，不要用。
      - Deepgram nova-3 是純 STT，沒有思考模式。

      **3.6 為什麼傳 `thinkingBudget` 會 400，官方文件沒有解釋**（查過
      changelog 與 deprecations 都沒有相關條目）。但實務結論很明確：
      3.x 一律用 `thinkingLevel` 就不會踩到。

      **處置**：兩家都改成「依序試候選寫法、成功就記住這個模型」
      （`providers.thinking_candidates` / `_REASONING_OFF`），
      並在回應裡檢查思考 token 是否真的是 0，不是就印警告。
      **不寫死模型名稱** —— 語法每一代都在變，寫死的清單一定會過期。
      `vocab.verify_readings` 也共用同一套，避免各處各自寫死。

      **Groq 的偏好順序也跟著改了**：qwen 排到 gpt-oss 前面，
      因為 qwen 的推理 token 真的是 0，gpt-oss 最低只到 `low`、
      每次還是燒 70 個。`groq/compound` 移到最後 —— 它不遵守
      `response_format: json_object`（會回散文），實測連短提示都回 413。

      關不掉的模型只警告一次（`_REASONING_FLOOR`），不要每次呼叫都洗版，
      而且訊息要說清楚是「這個模型的下限」而不是「設定沒生效」。

      補齊候選後實測，六個 Gemini 模型全部解析成功且思考 token 為 0：

          gemini-3.8-flash        thinkingLevel: LOW
          gemini-3.7-flash        thinkingLevel: LOW
          gemini-3.6-flash        thinkingLevel: MINIMAL
          gemini-3.5-flash        thinkingLevel: MINIMAL
          gemini-3.1-flash-lite   thinkingLevel: MINIMAL
          gemini-2.5-flash        thinkingBudget: 0

### 送 API 的批次大小

- [x] 查清楚上限：`gemini-3.8-flash` 輸入 1,048,576 tokens、輸出 65,536；
      `gemini-3.5-transcribe` 輸入 98,304、輸出 32,768
- [x] 算過一集的量：約 2500 字日文 ≈ 4000 tokens，只佔輸入上限的 0.4%
- [x] 翻譯改成整集一次送。實測 111 句一次送出 16 秒翻完、111/111 成功，
      舊做法分 3 批約 55 秒。整份失敗才退回分批，那時分批的價值是救回一部分

### 轉錄品質，照這個優先序做

**1. 總時長必須等於音檔長度** —— 這是硬性檢查，不通過就不要往下做
- [x] `backend/verify.py`：比對逐字稿時間範圍與音檔實際長度（ffprobe 讀網址即可，
      0.7 秒，不必下載）。涵蓋不足就當這家失敗、換下一家
- [x] 同時檢查中間有沒有超過 30 秒的空白 —— 這道檢查立刻抓到真問題（見下）

**2. 音軌與逐字稿對齊**
- [x] `verify.check_timeline`：時間倒退、單一詞橫跨過久、語速離譜三種徵狀，
      都是純看數字就能判斷的對齊問題。三種都用人工造的壞資料驗過抓得到
- [x] 時間戳超出音檔長度的檢查（原本只查「比音檔短」，
      「比音檔長」那種時間軸算錯的情況會整個溜過去）

**3. 內容準確性**

- [x] **填充詞、語尾助詞不會被清掉** —— 三家實測後改用 Gemini 的 verbatim 模式。
      同一集填充詞：Gemini 60、Deepgram 16、Whisper 14。
      實例：Gemini 轉出「え、東京の渋谷にえ、今年の2月にできたえ、トイレについて
      最近ですね」，Deepgram 把三個「え、」和「ですね」全清掉了。
      這確實是在「音檔轉文字」那一步就決定的，後面救不回來，你的擔心是對的。
- [x] **抓到並修掉一個嚴重的幻覺來源**：指定 `language=ja` 去轉英文段落時，
      Deepgram 不會跳過，而是把英文音節硬拼成假名
      （"thankyouismaybeyouronecoose。。。"），那串垃圾會被當成正常內容
      寫進逐字稿再拿去翻譯。雙語節目 バイリンガルニュース 有 45% 的內容如此。
      改用 `nova-3 + language=multi` 之後：空白從 50 段降到 0 段、
      字數 26,582 → 57,631、涵蓋 99.8%。而且純日文節目也沒變差 ——
      填充詞 16 vs 14、語尾助詞 51 vs 46，反而保留更多
- [x] 英文詞之間補空格（原本 "".join 讓英文黏成 "thinkitismaybeaonecause"）
- [x] **音樂與長靜默的幻覺**（2026-09-06，`verify.detect_silence_hallucination`）。
      重複偵測只抓得到「同一句講很多次」，但幻覺還有更安靜的形式：
      在沒有語音的地方生出**看起來完全正常**的句子，不重複也通順，
      光看文字發現不了。所以回去看音檔 —— ffmpeg 的 `silencedetect`
      直接給靜音區間，串流讀網址不必下載（11 分鐘節目 1 秒、40 分鐘 4 秒）。

      門檻是實測調的：`noise=-35dB` 而不是預設的 -60（podcast 有底噪與
      背景音樂，-60 幾乎抓不到）、`d=2` 秒（句子間的停頓通常在 1 秒內）。
      比對時兩端各留 0.5 秒，句尾尾音落進靜音區間是正常的。

      用人工壞資料驗過會抓（塞 14 個字進 401–405 秒的靜音 → 抓到），
      邊界情況也驗過不誤報（單一個字落進去 → 放行）。
      現有四集實測沒有誤報。
- [x] **斷詞錯誤已修**（`segment.refine_word_boundaries`，commit 5e5879c）。
      資料庫裡還看得到「その理／由として」「金／融システム」這種切法，
      但那四份逐字稿是 09-05 產生的、修正是 09-06 才進去的 —— 資料舊而已。
      實測退場路徑（長度對不齊就原樣回傳）在真實資料上不會觸發。
- [x] **找日文標準答案語料 —— 調查完畢，結論是不存在**
      掃了 1,873 個日文 podcast RSS，約 118 個（6%）有 `podcast:transcript` 標籤，
      但抽樣驗證後確認**全部是 Spotify 與 listen.style 的自動 ASR**，
      一個人工校對的都沒有（下載的 サイエントーク 那份把兩人對話全標成同一個
      Speaker）。NHK、読売、産経、TBS 這些新聞大廠也都沒有。
      日文 podcast 圈沒有「文字起こし公開」的文化，這點可以當定論。
      更糟的是「日英混雜 + 保留填充詞 + 人工校對」這個組合的公開資源等於零 ——
      日英 code-switching 語料只有兩個小規模的，NAIST 那個 28 萬句的是 TTS 合成。

- [x] `backend/make_eval_set.py`：把人工標註的成本壓下來。
      用兩家 ASR 轉同一段，兩邊講法一致的地方通常就是對的，
      人只需要看分歧處。實測 5 分鐘片段 55 句裡只有 21 句需要人耳判斷（38%），
      而且它當場抓到一個真錯誤：Deepgram 把「犯罪の防止」聽成「犯罪の帽子」
      （同音異義，讀日文很難發現，兩家一比就浮出來）。
      比對前會抹平標點、全半形與漢數字／阿拉伯數字的寫法差異，
      那些不是辨識差異
- [x] **改用 TEDxJP-10K 當標準答案，第一次量到客觀數字**
      273 場 TEDx 演講、人工校對過字幕與時間軸，2026/09 實測 273/273 仍存活、
      md5 比對 100% 相符。它特別適合這個專案 —— 人工修正的方向正是
      「把 YouTube 字幕清掉的填充詞加回去」，所以同時能量「聽錯多少」
      與「吃掉多少口語」。
      `backend/benchmark.py` 實測 80 句：
        引擎        字元錯誤率   填充詞      語尾助詞
        標準答案      —         26 個      21 個
        Gemini      11.2%     31 個(119%) 24 個(114%)
        Deepgram    18.9%      8 個( 31%) 21 個(100%)
      Gemini 抓到的填充詞比人工標註的還多，Deepgram 丟掉七成。
      限制：只有獨白（TEDx 演講），多人對話素材還缺
- [x] **TalkBank 已排除**（2026-09-06，使用者決定）。查過授權後不划算：
      CC BY-NC-SA 3.0 明文禁止商業使用，而且 ground rules 規定
      「data can only be uploaded for processing by OpenAI, Anthropic, Google,
      Rev-AI and similar systems **if the user has specifically selected the
      option for non-storage of data**」—— 我們的 benchmark 就是把音檔送給
      Gemini／Deepgram／Groq，等於要為了跑評測改一整套流程。
      多人對話的標準答案因此沒有來源，但影響有限：
      diarization 本身已驗證可用（173 分鐘對談辨識出 3 位、分布合理），
      而 CER 與填充詞保留率有 TEDxJP-10K 可以量。
- [x] **自標評測集不做**（2026-09-06，使用者決定）。需要一到兩天人力
      逐字打出 30–60 分鐘的 podcast，不符合「自己玩的專案」的投入規模。
      代價是評測素材仍然是 TEDx 演講體，跟實際的 podcast 口語有落差 ——
      CER 11.2% 這個數字要理解成「演講體的錯誤率」，podcast 應該更高。
- [x] **TEDxJP-10K 回歸測試不做**（2026-09-06，使用者決定）。
      語料要現場下載 273 場 YouTube 音檔，脆弱又慢；而回歸測試的價值是
      「偵測品質退步」，但轉錄那條線目前不常動。已經量過的 11.2% CER
      留著當基準數字就夠了。
（原本這裡有一項「用 TalkBank CallHome 驗填充詞」，隨上面一起排除。
填充詞保留率已經用 TEDxJP-10K 量過：Gemini 119%、Deepgram 31%。）
- [x] **CSJ 不做**（2026-09-06，使用者決定）。商業授權 ¥500,000 起，
      這是自己玩的專案，不成立。

- [x] **試過把中文加進 `language_codes`，結論是不要加**（2026-09-06）。

      起因：中日雙語節目（日本人森 EP45）的中文段落被轉成垃圾
      （「hello但是好徐理是理붴를세요。」），看起來跟當初少了英文時
      バイリンガルニュース 有 36% 是垃圾是同一類問題。

      但實測否決了這個推論。同一段 50 秒中日混講：

      | language_codes | 詞數 | 涵蓋 | 結果 |
      |---|---|---|---|
      | ja-JP + en-US | **123** | **49.9/50 秒** | 日文完整 |
      | ＋ zh-TW | 117 | 41.3 秒 | 變差 |
      | ＋ cmn-Hans-CN | 65 | 26.9 秒 | 更差 |
      | ＋ 兩種中文 | 64 | 49.6 秒 | **日文全丟，只剩中文** |

      **Gemini 不是「多語言一起處理」，而是挑一個主導語言、其餘丟掉。**
      加了中文之後它改挑中文 —— 那正好是最糟的結果，因為要學的是日文。

      所以維持 `["ja-JP", "en-US"]`。中文段落轉不好是可接受的代價
      （使用者本來就懂中文），日文被丟掉不行。

      **這一集的正確處置是讓涵蓋檢查擋下 Gemini 再退回 Deepgram** ——
      實際上系統就是這樣做的（Gemini 中間空白 29%、沒過，自動換手）。
      機制是對的，只是這種節目的品質天花板比較低。

### 多人辨識（不強求）

- [x] 逐字稿標出誰在講話。不需要事先指定人數，兩家都是自動偵測。
      實測 173 分鐘對談節目辨識出 3 位，分布 67%／17%／15%，
      對應主持人、共同主持人與來賓。speaker 已寫進資料庫
      （純新增欄位，舊版 App 忽略即可）
- [x] **App 端怎麼呈現說話者 —— 建議已定案**（2026-09-06，使用者授權照建議做）。
      實作在 Mac 那邊，規格記在 `HANDOFF-MAC.md`。

      先看實際資料，因為它直接否決了最直覺的做法：

      | 集數 | 說話者 | 切換頻率 |
      |---|---|---|
      | 大人の日本語 | 1 位 | 0 次／120 句 |
      | ニュース小話 #230 | 2 位（52%／48%） | **236 次／674 句** |
      | EP45 日本人森 | 2 位（77%／23%） | 40 次／133 句 |

      **平均每 3 句就換一次人。** 所以用背景色分色會讓整個畫面一直閃，
      而且會跟「目前這句」的高亮打架 —— 那是這個 App 最重要的視覺訊號，
      不能被稀釋。

      定案的做法：

      1. **只有一位說話者時完全不顯示。** 獨白節目佔多數，
         為一個不存在的區別加 UI 是純粹的雜訊。
         判斷方式：整份 `lines` 的 `speaker` 只有一種值就整個關掉。
      2. **多人時用前緣細線，不用背景色。** 2–3pt 的直線畫在句子左側，
         顏色取自 Theme 的中性色階。細線不會影響日文的可讀性，
         背景色會。
      3. **只在「換人」的那一句畫線**，連續同一人的句子不重複畫。
         這跟聊天介面把連續訊息併成一組是同樣的道理，
         也讓 236 次切換在視覺上變成 236 個安靜的分隔而不是 674 個色塊。
      4. **名字讓使用者自己標。** 我們拿不到真名（diarization 只給編號）。
         長按某句 →「這是誰在說話」→ 輸入名字 → 套用到該集所有同編號的句子，
         存在本機（跟「聽到哪」一樣屬於 UserDefaults 的東西）。
         沒標名字就只有顏色，一樣能分辨誰是誰。
      5. **不要做頭像或大標題。** diarization 會出錯，
         細線的視覺份量剛好對應它的可信度；頭像加名字會讓錯誤看起來像事實。

### 架構評估

- [x] **`gemini-3.5-transcribe` 實測完畢並納入架構，排在第一順位**
      關鍵是 `"type": "verbatim"` 模式：同一集填充詞保留 60 個，
      Deepgram 16、Whisper 14。對「聽 podcast 學日語」這是功能差異不是邊際改善。
      代價是慢十倍（30 秒 vs 3 秒）。
      實作細節：詞級時間戳只有 `/v1beta/interactions` 端點支援
      （generateContent 傳 transcription_config 會 400）；音檔走 inline base64
      而非 File API（File API 的檔案綁金鑰，跟多金鑰輪替打架）；
      講者資訊只掛在 word annotation 上，單獨開 diarization 會拿到空的。
      時間戳可信範圍實測：31 分以內乾淨、35 分開始缺尾、45 分出現鬼值、
      55 分只轉到 63%、60 分直接 400 —— 所以切在 28 分。
- [x] **`language_codes` 必須顯式傳 `["ja-JP","en-US"]`，這是硬性條件**
      Gemini 沒有「多語言模式」開關：不指定語言的行為與指定單一語言完全相同，
      自動偵測只挑一個主導語言、其餘整段丟棄。
      同一段 60 秒實測：Deepgram 轉出 320 個拉丁字母
      （"so i guess you're helping the local people..."），
      Gemini 不傳語言只剩 13 個（"Theobstacleof"）—— 整段英文沒了。
      而它的失敗比 Deepgram 更難察覺：Deepgram 會拼出假名垃圾（看得出來），
      Gemini 是直接沒有那些詞，但日文段落跨在前後，涵蓋率仍顯示 99.8%。
      **光看 coverage 這個失敗是隱形的**，只有 verify 的空白比例攔得住。
- [x] 但它不是永遠可靠：`日本語の森` 那集只轉出 460 詞（Deepgram 1305），
      中間漏掉 56%、語速每秒 0.8 字。涵蓋驗證擋下來並自動退到 Deepgram ——
      沒有那道檢查的話，那份殘缺逐字稿會直接進資料庫且表面正常
- [x] 三家同一集實測比較完畢（見上方各項數據），已定案 Gemini 為第一順位、
      Deepgram 次之、Whisper 最後，任何一家的結果沒通過驗證就自動換下一家
- [x] **雙轉錄交叉驗證不做**（2026-09-06，使用者決定）。
      成本翻倍（每集要轉兩次），但它想抓的「辨識出不存在的詞」
      已經有免費的替代方案 —— 離線字典查不到的詞就是那個訊號
      （見階段 4「意外的收穫：查不到的詞就是轉錄錯誤」）。

## 階段 4：語言學習功能

這一段整體要等你想清楚「2026 年搭配 AI 的學習模式該長什麼樣」再定案。
以下是已經查證過的技術現況，先擺著當決策依據。

### 假名標注

你的要求是**零容忍**。先講結論：**單靠自動化做不到 100%，但可以做到「知道自己何時不確定」**。

已查證：

- 業界標竿 **Satori Reader 的假名是純人工標註的**（內容從撰寫、編輯、錄音到標註
  全部由人做）。做得最好的產品選擇不自動化，這本身就是答案的一部分。
- 自動生成的準確度天花板約 **98.8%**（EZFurigana 公佈的 token 級數字），
  錯的集中在人名、地名、連濁、罕見詞。
- 本機實測 SudachiPy（`sudachipy` + `sudachidict_core`，純 Python 免編譯）：
  同形異音的困難案例 12/14 正確。「駅に行った→イッ」與「式を行った→オコナッ」
  兩種讀法都判斷正確，上下文有效。
  錯的兩個是「明日→アス」（あした／あす都合法）與「市場→シジョウ」
  （いちば／しじょう都合法）—— **兩個都是「有多種正確答案」而非讀錯**。
- Sudachi 同時給出 `dictionary_form`（原形），那正是查字典需要的東西。

**所以「100% 正確」這個目標要重新定義。** 有些詞本來就有多種讀法，
連日本人都會分歧。可行的方向是交叉驗證：形態素解析與 LLM 各標一次，
**兩邊一致才顯示，不一致就標記為「有多種讀法」並列出候選**。
對學語言的人來說「這裡有兩種讀法」本身就是有價值的資訊，
比默默標錯一個好得多。

- [x] **後端假名標注完成**（2026-09-06，在 `backend/vocab.py`）。
      Sudachi 標讀音，再用 LLM 覆核有多讀音疑慮的那批。

      **為什麼值得打這一次 API**（先量再決定，不是預設要用）：
      Sudachi 對最高頻的字有系統性誤標 ——

          私  Sudachi 給 わたくし，口語實際是 わたし
          方  Sudachi 給 ほう，「怒っている方」其實是 かた
          何  Sudachi 給 なん，「何が問題」其實是 なに

      四集實測，光這幾個字就佔含漢字詞的 **2.9%**。
      三個模型對這類分歧的判斷 12/12 全部正確。

      **用最便宜的模型就夠**：`gemini-3.1-flash-lite` 與 Sudachi 一致率 83%、
      `gemini-3-flash-preview` 82% —— 大模型沒有比較好。實際跑四集分別
      改掉 14／113／14／49 處。一集一次呼叫、約 2,700 in ／1,900 out tokens、
      關閉思考模式、用 responseSchema 固定格式。

      **字典當防護網**：只有當 LLM 給的讀音確實是該詞在 JMdict 裡的合法讀音
      時才採用。四集分別擋掉 4／23／6／14 個模型編出來的讀音。

      **讀音記在出現位置不是記在詞表**：同一個「方」在這句是かた、
      那句可能是ほう。所以覆核後的讀音接在 `tokens` 的 span 第四個元素，
      App 看到就覆蓋詞表預設值。

- [x] **不確定時顯示候選 —— 已定案並實作**（2026-09-06，使用者同意）。

      原本打算用 `alt` 欄位當觸發條件，但那是錯的：`alt` 只代表
      「這個詞形在字典裡有別的讀音」，306 個詞裡有 110 個（36%）會中，
      照那樣做 App 三分之一的詞都掛著候選，變成雜訊。

      真正的不確定是**兩個來源意見不同**。所以改成問兩個模型：

      - 兩邊一致 → 直接用，不顯示候選
      - 兩邊一致但與 Sudachi 不同 → 用模型的（實測這種情況模型 12/12 正確）
      - **兩邊不一致 → 讀音記成 `"?"`，App 顯示候選**

      實測一集 139 個疑慮詞裡 6 個（4%）落在這裡，比例合理，而且都是真歧義：
      皆（みな／みんな）、街中（まちなか／まちじゅう，意思不同）、
      避け（さける／よける，是兩個不同的動詞）、他（た／ほか）。

      多問一次的成本可以忽略（一次 2,700 tokens、用最便宜的模型），
      換到的是「知道自己什麼時候不確定」—— 那正是零容忍要求下真正需要的東西。

### 點單字查字典 —— 做完了，離線可查、零 API

你的要求是**離線字典，完全不經過模型**。已經做出來了：`backend/dictionary.py`。

    python backend/dictionary.py --build      # 下載 EDRDG 資料建成 SQLite
    python backend/dictionary.py --lookup 市場 --reading シジョウ

|  | 條目 | 詞形 |
|---|---|---|
| JMdict（一般詞彙） | 218,672 | 465,316 |
| JMnedict（人名地名） | 743,624 | 954,728 |
| **合計** | **962,296** | **1,369,667** |

170 MB SQLite，查詢是索引查找，0 API 呼叫、可離線。
**拿五集真實逐字稿實測，詞次涵蓋率 96.6%。**

#### 建這本字典時撞到的四個坑（都會靜默失敗，所以記下來）

1. **只索引漢字，純假名的常用詞就全查不到。**
   JMdict 的 lemma 是「沢山」，但口語裡幾乎都寫「たくさん」。
   漢字與假名寫法都要建索引，詞形數從 29.6 萬變 46.5 萬。
   `ちょっと`、`なんか`、`ありがとう` 全部屬於這一類。

2. **JMnedict 一個詞形有 3–9 個同形異讀，而且沒有頻率標記。**
   直接取第一個會拿到最冷僻的：`名古屋`→たなごや（罕見姓氏，不是名古屋市）、
   `佐藤`→さいう、`野村`→いむら。
   解法是拿 Sudachi 的 `reading_form()` 去比對 —— 那是**該語境的實際讀音**，
   轉錄流程裡本來就算出來了，不必額外做任何事。實測 5/5 全部挑對。

3. **可能形查不到。** Sudachi 不把 `導き出せる` 還原成 `導き出す`，
   而 JMdict 大多沒收可能形。加 e 段→u 段的還原（歩ける→歩く、
   食べられる→食べる）。只在直接查不到時才還原，所以 `いける`
   這種本身就在字典裡的詞不會被誤判成 `行く`。

4. **不要用 `normalized_form`，一律用 `dictionary_form`。** 理由同上。

#### 意外的收穫：查不到的詞就是轉錄錯誤

實測那 3.4% 查不到的東西，扣掉數字與英文縮寫之後，剩下的幾乎都是聽錯：

    エアイ      ← AI
    カロナイナ   ← カロライナ（Carolina）
    ガルボット   ← ロボット
    是是        ← 根本不是詞

先前這裡寫著「A 類錯誤（辨識出不存在的詞）要靠 LLM 擋下」。
**不用。字典查不到就是最強的訊號，而且零成本。**
真正需要語境判斷的只剩 B 類 —— 聽成另一個真實存在的同音詞
（感心↔関心、異動↔移動、公演↔講演），那類字典抓不到，
因為字典沒有說謊，只是那句話講的是另一個詞。

#### 中文釋義：Tomoshi（找到了，不用自己翻）

**tomoshi-app/tomoshi-dict-data** —— JMdict 全量的**繁體**中文釋義，
21.7 萬條目 / 25.1 萬義項，**CC BY-SA 4.0**，逐表都有授權宣告。
產製時用 LLM 輔助翻譯，但成品是靜態表，我們查詢純查表、不碰模型。

    https://github.com/tomoshi-app/tomoshi-dict-data
    sha256 cd4e6b71...（`dictionary.py` 會自動核對）

實測（**用我們自己的 podcast 逐字稿，不是通用語料**）：

    合計 7,314 詞次：查得到 96.6%、有中文釋義 96.0%

**加上中文這層幾乎沒有損失**（96.6% → 96.0%），差的那 0.6% 是人名地名，
本來就只有 JMnedict 有。抽驗填充詞也有收：`あのー` → 嗯；呃；那個。

**為什麼不拿它的簡體表自己 OpenCC 轉？** 因為會轉錯約 2,000 條。
實測（拿它的簡體表轉了跟人工繁體版比對，4,659 組義項）：
`s2twp` 一致率 99.2%，`s2t`（不帶台灣詞彙映射）差 11 倍。
那 0.8% 錯得很系統性，而且正是最難發現的那種：

- **一簡對多繁選錯字**：岩手→巖手、血症→血癥、影像→影象、想像→想象
- **詞彙映射過頭**（最多）：通過→透過，於是「考試合格；通過考試」
  變成「透過考試」。人工版分得出來 —— 「合格」用通過、
  「通る」的穿透義才用透過，OpenCC 分不出來。
- **台灣用語沒轉到**：激光→鐳射（該是雷射）、实时→實時（該是即時）

**注意 pip 上有兩個 opencc，裝錯會得到完全不同的結果。**
`opencc-python-reimplemented` 內建的是很舊的字典快照，
會把「岩手県」轉成「巖手縣」、「通過」轉成「透過」、「血症」轉成「血癥」。
官方的 `opencc`（1.4.2）這些全部答對。我一度以為那些是資料本身的瑕疵，
其實是我環境裡兩個套件都裝著、量到了舊的那個。

品質稽核用 `--audit`，不要列硬編碼的錯字表：

    python backend/dictionary.py --audit

它做兩件事，都是量出來的不是猜的：

1. **字形**：拿 Tomoshi 的簡體表用官方 OpenCC 重轉跟繁體表比對。
   實測 **315 筆 / 25.1 萬義項（0.13%）**，自動排除 137 個日文原文偽陽性。
2. **詞彙替換**：s2twp 的 `TWPhrases` 有 819 條規則，其中一部分的簡體原詞
   在非科技語境有別的意思（文件也指公文、程序也指步驟、默认也指默許）。
   查這些規則開火在什麼語境上 —— **1,729 個條目，其中常用詞 213 個（0.71%）**。

第二類是重點：規則本身沒錯（電腦的 file 就是檔案），錯在不看語境無差別套用。
這張表跟被拿掉的 `_ZHTW_FIXES` 的差別在於**它是有界的** ——
規則總共 819 條，檢查得完；錯字是無界的，永遠不知道漏了多少。

實際錯誤的量級（人工抽驗 24 個常用詞，22 個完全正確）：

    ✗ 藩主或將軍的簽署文件 → 簽署檔案      錯
    ✓ 歸檔程序           → 歸檔程式      對
    ✗ 黙認：預設；默許…                 「預設」是 默认→預設 套錯語境

而且錯誤集中在**科技詞彙**（文件、程序、通信、对象、智能），
對「聽 podcast 學日語」影響很小 —— 那些詞在日常對話幾乎不出現。

**已修的一類**：詞彙替換會讓兩個原本不同的詞撞成同一個
（「通信情报；通讯情报」→「通訊情報；通訊情報」），實測 1,831 個條目、
常用詞佔 904 個（3.0%）。`_import_chinese` 現在會在**詞的層級**去重
（不是字串層級 —— 單一個 gloss 自己就帶分號），降到 1,184，
剩下的是跨義項的正常重複，不該刪。這是規則不是清單，不用維護。

**不一致不等於錯，所以不做自動修正** —— 實測三種情況都有：

| 差異 | 誰對 |
|---|---|
| 託兒所 → 托兒所 | 官方對 |
| 幼稚園 → 幼兒園 | **Tomoshi 對**（官方 s2twp 轉成了大陸用語） |
| 衝浪 → 沖浪 | **Tomoshi 對** |
| 這周 → 這週 | 官方對（Tomoshi 同一條裡「這周」「本週」並存） |

兩邊各有對的時候，所以沒有自動解，只能一筆一筆看。

**不會有香港用語**：OpenCC 的 `HKVariants` / `HKPhrases` 完全不在
`s2tw` / `s2twp` 的轉換鏈路裡，港式字形與詞彙進不來。沒轉到的詞
是**大陸用語**以繁體字形留著（軟件、網絡、信息這些港陸共用，
所以看起來像港式）。真正的港式獨有詞（的士、雪櫃）簡體原文根本不會有。

**其他找過的都不能用**（兩路 agent 分頭查證）：

| 資源 | 條目 | 為什麼不用 |
|---|---|---|
| lxl66566/Japanese-Chinese-thesaurus | 11,638 | 規模只到 N5–N2，且來源未列、格式看得出是商業辭典 |
| bg1fpx/JECD | 54,889 | 無授權宣告，每詞只有一條四字釋義 |
| carlcc/NihongDict 系列 | 2–27 萬 | 明顯是商業辭典 StarDict 化，版權不行 |
| 小学館中日辞典 / 中日大辞典（Yomitan、FreeMdict） | 14–17 萬 | 全是掃描商業辭典，而且方向是中→日 |
| Sakura286 dict-ja-zh-wiktionary | 79,597 | 就是 zh.wiktionary，涵蓋率 67.5% 已否決 |
| HuggingFace / Kaggle / ModelScope | — | 沒有日中詞典資料集，只有翻譯用的平行語料 |

**CC-CEDICT 的日中對應物不存在。** 中英之間有 CC-CEDICT，日中這邊沒有人
做過等價的事，EDRDG 官方也沒有中文分支。Tomoshi 是目前唯一的解。

- [x] 找到中文釋義來源並驗證涵蓋率
- [x] App 裡放出處標示（2026-09-06 完成）。設定頁多了「字典來源」一區，
      列 EDRDG（JMdict/JMnedict）與 Tomoshi (Y1Z)，都標 CC BY-SA 4.0。
- [x] **字典走後端不綁進 App**（2026-09-06 完成）。做法是
      `backend/vocab.py`：轉錄時就把該集的詞表與**詞邊界**查好，
      存進 `kikitori_transcripts.vocab`，跟逐字稿一起下載。
      實測一集 87–371 KB。零 API 呼叫（Sudachi + 離線 SQLite 都在 runner 本機）。

      **詞邊界是關鍵**：日文沒有空格，App 沒辦法自己斷詞，
      所以 `tokens` 欄位（每句的 `[起, 迄, 原形]`）不是附加品而是必要的。
      沒有它 App 連「使用者點到哪個詞」都判斷不出來。

      過程中修掉兩個會讓假名標注失準的 bug：
      - Sudachi 的 `reading_form()` 是**表面形**的讀音（「言わない」回イワ），
        拿去比對字典裡的原形讀音永遠對不上。要用原形自己的讀音查，
        一集 306 個詞裡有 83 個中招 → 修完剩 2 個。
      - 外來語的讀音在 JMdict 存成片假名，只把輸入轉平假名會對不上
        （トイレ、オフィス、ニュース）。兩邊都轉再比。

      多讀音的詞會帶 `alt` 欄位給 App 顯示候選，但**只取 JMdict**——
      混進 JMnedict 的人名讀音會變成雜訊（「日」列出あきら、「風」列出かおる）。
- [x] **Tomoshi 備援已存**（2026-09-06）。它是單人維護的新專案（2026-08 首版、
      GitHub 1 star），上游若消失就重建不了。原始檔留在
      `backend/data/cache/tomoshi.db.backup`（570 MB，不進版控）。
      `--build` 會核對 sha256，所以拿到的一定是驗證過的那一份。

#### 支語（大陸用語）：全量掃描，無歧義的已自動換掉

使用者對支語非常敏感，而 Tomoshi 的繁體是簡轉繁來的 —— s2twp 只有 819 條
台灣詞彙映射，轉不到的大陸用語就以繁體字形留著（賬戶、綜合徵、摩托車、營銷）。

詞表用 **sysprog21/zhtw-mcp**（MIT，可商用），1,673 條 cross_strait 規則。
公開詞表查過六份，去重後共 4,609 條；選這份是因為它有 `english` 與
`context_clues` 兩個欄位，可以做語境判斷，其他幾份只有詞對詞。

**分成兩批，分界不是我判斷的，是規則資料自己標的**：

| | 規則 | 處置 |
|---|---|---|
| 沒有 `context_clues`（作者認為無歧義） | 1,353 條 | 自動替換，實際開火 336 條、3,080 處 |
| 有 `context_clues`（質量、原子、模擬） | 308 條 | 只標記，`--audit` 列出 |

套用時還有兩道保護：

1. **英文釋義要支持這條規則**。「質量」只有在 JMdict 英文真的講 quality
   時才算命中。純字面比對會誤判 5.38% 的條目，加上英文確認降到 2.41%。
2. **台灣說法已經在同一義項就不換**。辭典會刻意並列兩岸說法
   （「計程車；出租車」），換掉會先變成「計程車；計程車」再被去重吃掉，
   等於刪資料。「話筒；麥克風」更嚴重 —— 話筒還有聽筒的意思。
   實測這種並列有 415 處、涉及 121 條規則。

**試過但收回的做法**：加一條「至少要有第二份詞表背書」（六份公開詞表裡
77% 的詞只有單一來源）。實測後發現它篩的不是正確性，是「剛好有沒有在
OpenCC 的 IT 詞表裡」—— `低級→低階`、`彙編→組譯` 這兩條可疑的反而通過，
`賬戶→帳戶`、`摩托車→機車` 這些無疑問該換的卻被擋掉。加了更糟。

取而代之：**每一次替換都記進 `data/zhtw_applied.json`**，336 條規則
逐條可審。規則是有界的，不像錯字清單是無界的 —— 這是跟被拿掉的
`_ZHTW_FIXES` 的根本差別。

**理論上限（查證過）**：支語本質上是開放集合，補不完。六份詞表裡 77% 的詞
只有單一來源背書，而且有 104 條詞同時出現在「該改的陸側」與「要改成的台側」，
各份資料互相打臉。高信心無爭議的替換大約就是 3,000–4,000 條，再往上誤報
成本超過收益。最難抓的「同名異實」（土豆、窩心、公車）字面掃不出來。

#### 中文釋義的來源：查遍了，只有兩條路

使用者問「台灣應該有很權威的日文 App，他們用什麼字典」。查證結果：

**台灣的日文 App 確實用原生繁體辭典，但都是買授權的：**

| App | 辭典來源 |
|---|---|
| Yahoo 奇摩字典 | Dr.eye 譯典通 → 收錄**《淡江日華辭典》** |
| 新時代日漢辭典 | **大新書局**授權（8 萬餘詞，繁中原生，含語調標記） |
| MOJi辞書 / 滬江小D | **查不到公開的資料來源說明**（滬江的英文部分有標柯林斯授權，日文部分沒有） |
| Jisho / Takoboto / imiwa? / Akebi | 全部是 JMdict，跟我們一樣 |

**開源的原生繁中日中辭典：不存在。** 這件事查得夠徹底了：

- 國教院樂詞網、教育部各辭典、中研院、data.gov.tw —— **完全沒有日語**
- 兩岸常用詞典（99,678 條）還活著且授權給萌典，但**沒有日文，而且是非營利授權**
- Yomitan 生態系官方辭典清單**沒有任何日→中辭典**
- 成大有「台灣日語學習者語料庫 CTLJ」，但那是收台灣人寫的日文做偏誤研究，不是辭典

**線上辭典 API 也是死路：**

| 服務 | 現況 |
|---|---|
| goo辞書 API | **2025-06-25 停止服務**（NTT 收掉） |
| Weblio / kotobank | 沒有官方 API |
| Jisho 非官方 API | 站方明確反對爬取，10 req/10 秒就 429 |
| Yahoo! JAPAN 文字解析 | 是解析不是辭典，沒有釋義，原則上不可商用 |

而且線上 API 跟「離線塞一本字典」的需求本來就相反。

#### 兩條路的取捨

**A. 繼續用 Tomoshi**（現況）
21.7 萬條、免費、CC BY-SA、已經在跑。已知瑕疵 1.74%（常用詞 3.76%），
其中重複義項已修。缺點是語意正確性無法驗證 —— 沒有第二本日中字典當基準。

**B. 談大新書局的授權**
8 萬餘詞、繁中原生、含**標準語調與詞性變化語調**（那正好是階段 4 假名標注要的東西）、
用例經學者潤飾。已有先例：MeBook 曉騰國際拿到授權、Soyong 開發、
在 iOS/Android 賣 US$6.99（`tw.com.mebook.dhdic`）。
大新 1960 年成立，1983 年起就在跟日本、中國簽出版授權契約，有處理版權的體制。
沒有公開的授權窗口，電話 (02) 2707-3838。

規模比 Tomoshi 小（8 萬 vs 21.7 萬），但那 21.7 萬裡有大量冷僻詞；
8 萬對學習用途夠，而且品質是人編的。

- [x] **B 已否決**（2026-09-06）。使用者原話：「談大新書局的授權不可能，
      我只是做自己玩的，不可能做到這個程度」。所以路線定了：**留在 Tomoshi**，
      靠 `--audit` 掌握瑕疵、靠自動替換清掉無歧義的支語，
      中文有疑慮時看同一筆結果裡的 JMdict 英文釋義。不要再提授權這條。

**順帶記下另一個被否決的**：查詢時即時打線上辭典（使用者提過當下下策）。
線上辭典沒有可用的 API —— goo辞書 2025-06 停止服務、Weblio 與 kotobank
沒有 API、Jisho 官方明確反對爬取（10 req/10 秒就 429），而且那跟「離線字典」
的前提相反。**但開連結給使用者自己看完全沒問題**，那不是抓取：

- [x] App 端的外部連結做好了（2026-09-06）。詞義卡片底下有 Weblio、Jisho、
      日中辭典（`cjjc.weblio.jp`）三個按鈕，只做 openURL 不抓資料。

#### 自我評分（2026-09-06）—— 留著當後續改進的參照

使用者的標準：離線零模型、不要個案補丁、不要抽樣要全量、對支語敏感。

**第一版 6 分。** 引擎是對的（離線、零 API、涵蓋 96.6%、(詞形,讀音) 雙鍵
解決同形異讀、可能形還原、假名索引），但有三個直接違反已經講過的原則：

| 問題 | 嚴重度 |
|---|---|
| `_ZHTW_FIXES` 硬編碼九條 | 使用者早就說過討厭這種做法 |
| 報「23 筆 / 0.009%」並宣稱「乾淨」 | **最嚴重** |
| 支語完全沒查 | 賬戶、摩托車、綜合徵、營銷全都在裡面 |

第二項扣最多。真實數字是千筆量級，少報兩個數量級，而且用了「乾淨」這個詞。
**錯不在數字不準，在於拿抽樣的結果講確定的話** —— 那會讓其他數字
全部失去可信度。使用者剛好追問才被翻出來，沒問的話那個假數字會一直留在這裡。

**目前 8.5 分。** 補上的：硬編碼拿掉、全量掃描（每一筆寫進 `audit.json`）、
支語 3,080 處已換（336 條開火規則寫進 `zhtw_applied.json` 逐條可審）、
重複義項詞層級去重。扣的 1.5 分：

1. **語意正確性仍無解**（-1）。2.62% 裡的「有歧義的詞彙替換」1,729 條與
   「陸用語需語境」2,016 條只能列出來，判定不了。原因是沒有第二本日中
   字典當基準 —— 但「無解」跟「解決了」對這個標準來說還是差很多。
2. **走過兩段冤枉路**（-0.5）。ECDICT 樞紐比對花了不少時間才確認是死路
   （15.4% 偽陽性）；共識門檻的設計是錯的（篩的不是正確性，是「剛好有沒有
   在 OpenCC 的 IT 詞表裡」），雖然是自己量出來並收回的。

**還沒驗證的**：字典還沒接進 App。實際點一個詞長什麼樣、反應多快、
293 MB 怎麼給 App，都還是紙上的。真正的分數要等接起來才算得準。

**後續順序的建議**：先把字典接進 App 看實際效果，再回頭處理那 2.62%。
接起來之後才知道哪些問題真的礙眼、哪些其實看不到，比現在猜準得多。

**不要再犯的**：

- 抽樣的結果不要用確定的語氣講。要嘛全量，要嘛講清楚是抽樣。
- 不要列「我想得到的錯字」。規則是有界的可以審，錯字清單是無界的。
- 設計了過濾條件要先量它篩掉了什麼，不要假設它有效
  （共識門檻看起來很合理，實際上篩錯東西）。

#### 「查了資料」有三種不同的失敗，一種比一種難察覺

跟 Mac 那邊對照出來的，記著當判準：

1. **沒查就下結論。** 最明顯。例：判斷轉錄退回 Deepgram 是「撞到配額」，
   那是推論不是證據，真正的原因是涵蓋檢查沒過。
2. **查了，但證據被自己改造過。** 在轉錄輸出加 `| tail -35`，
   日誌開頭被截掉，於是看到「直接從 Deepgram 開始」就判斷成
   「Gemini 沒被嘗試」。Mac 那邊的對應版本是 macOS 沒有 GNU 的 `timeout`，
   指令沒執行，但同時在背景取樣的資料看起來很有說服力 ——
   那份資料只證明了「沒人按下按鈕」。
3. **量測工具本身有 bug。** 最難察覺，因為前兩道防線都過了。
   例：查「沒中文的詞是上游缺還是我比對失敗」，腳本只檢查
   「zh_defs_zhtw 有沒有那一筆」、沒檢查「那一筆有沒有釋義」，
   得出跟事實相反的結論，差點照著去改沒問題的比對邏輯。

4. **檢查工具本身不可能觸發。** 比第三種更難發現，因為它**不會給錯誤答案，
   只會一直說「通過」** —— 沒有任何徵狀，而且讓人以為自己有防線。
   例：`verify.check()` 先把詞按 start 排序，再把排序後的結果傳給
   `check_timeline` 去檢查「有沒有時間倒退」。排序正好消滅了它要找的訊號，
   所以那個檢查從第一天起就不可能叫。代價是一份 64 句 end<start、
   41 處時間倒退的逐字稿通過驗證寫進資料庫。

   成因很反直覺：**排序不是 bug，是正常的前處理**，壞在它剛好消滅了
   下一步要找的訊號。這種「兩個各自正確的步驟湊在一起變錯」，
   讀單一函式時看不出來。

**判準：結論太乾淨、或讓接下來的工作變簡單時，要特別懷疑。**

那次得到「この/そう 是我們比對失敗」，不只乾淨，還剛好把責任指向
自己可控又最容易修的地方 —— 舒服本身就是警訊。反過來，如果量測結果
指向一個麻煩的方向（要去查上游、要跟資料源打交道），那個結論比較可能
是真的，因為沒有動機去偏袒它。

Mac 那邊的補充：真實世界的資料很少那麼整齊，太整齊通常代表
量到的不是真實世界（它那次量到「完全沒有倒退」，零倒退比
「偶爾倒退」乾淨太多）。

**還有兩個成本很低的習慣**（Mac 提的，實測有效）：

- **看到資料有問題，先撈 `created_at` 跟相關 commit 的時間比一比。**
  Mac 用今天的 `segment.py` 去解釋 09-05 產生的逐字稿，
  推論完全合乎邏輯但前提錯了 —— 等於拿今天的程式碼審判昨天的輸出。
- **主動去走那些從來沒被觸發過的退路。** 「我以為有防線」跟
  「真的有防線」的差別只有實際走一次才知道。實測三條：
  字典檔不存在（安靜回空，但 stats 會講）、沒有金鑰（原本靜默，已補 log）、
  轉錄模型全不在（明確拋錯）。

#### SudachiPy 斷詞的已知邊界

整體很好 —— 時態、敬語、複合動詞、外來語動詞（ググった→ググる）全對。
少數口語縮約會誤析（すんません、行かされた），低頻可接受。
SplitMode 用 C 為主（`公衆トイレ`、`持続可能性`、`ワーキングホリデー`
JMdict 都有整詞），查不到再 fallback 到 A 拆成子詞。

### 生字本與重點句收藏

你說這兩個其實是同一件事，而且想針對「2026 年搭配 AI 的學習模式」多想一下。
這一項等你的想法，我不先預設做法。

- [ ] **暫緩**（2026-09-06，使用者：「先放著」）。等他想清楚
      「2026 年搭配 AI 的學習模式」該長什麼樣再回來。
      技術上沒有阻礙 —— 詞表、釋義、假名都已經在 `vocab` 欄位裡了。

### 逐句重聽與側邊小助手

- [x] 逐句重聽：點逐字稿某一句，Spotify 就跳到那句開頭。
      UI 測試實測：點第 58 句（326.7 秒），Spotify 跳到 327.5 秒，誤差 0.8 秒。
      （順帶確認帳號是 Premium，否則控制播放會 403）
- [x] **側邊小助手**（2026-09-06）：浮在逐字稿右緣的控制條，切換點一句話要做什麼 ——
      「播放」點一句跳到那裡、「查詞」點一個詞看意思。兩件事本來搶同一個手勢，
      而日文的詞佔滿整行、沒有真正的空白處可以分流，所以改成明講的模式切換。
      兩種模式下長按都能「從這裡開始播」。
- [x] 逐句重聽（2026-09-06）：側邊小助手第二組控制，播到句尾自動跳回句首，
      直到自己往下或關掉。實測 Spotify 的位置鎖在 251.7~258.8 秒之間循環，
      倒退 5 次每次回頭約 5 秒，剛好是一句話的長度。
      開著重聽時點別句就換成重聽那一句。
- [ ] （原本記的）側邊小助手播放模式：預設是正常播放，
      另一種是逐句重聽模式。目前先做這兩種，之後再擴充。
      這是你明確講過的設計，但先前漏記了。

## 階段 5：體驗與可觀測性

- [x] App 遙測回傳：錯誤與關鍵事件寫進 `kikitori_logs`，
      開發時直接查得到裝置上發生什麼，不必靠截圖轉述
      （只送技術事件，device_id 是隨機碼，App 只能寫不能讀）
- [x] `backend/status.py`：一次看完轉錄任務、逐字稿存量、App 紀錄、
      憑證剩餘天數、CI 狀態
- [x] 排隊改為即時觸發（Supabase 觸發器 → GitHub，實測 3 秒開工）
- [x] 每日健康檢查排程：憑證快到期、卡住的任務、SideStore 修好沒 → 推 Discord
      （`backend/healthcheck.py`，每天台北 9 點跑，沒事就安靜）
- [x] Claude Code 這端掛 hook：push 後自動盯 CI，失敗把摘要寫進
      `build/ci-last-failure.log` 並跳通知（`.claude/settings.json` + `scripts/ci-watch.sh`）

- [x] 換掉 App 圖示（波形，配色取自 Theme；`scripts/make-icon.py` 可重新產生）
- [x] 排隊中的畫面看得到進度階段（尋找音檔 → 轉錄中 → 斷句 → 翻譯 N 句 →
      寫入資料庫），App 輪詢也從 20 秒縮到 8 秒才跟得上
- [x] 長按選單閃爍（contextMenu 沒有固定預覽，跟著高亮狀態重繪）
- [x] 自動推估時間軸偏移（Spotify 與原始音檔的長度差），校正過的偏移會記住
- [x] 輪詢 5 秒縮短為 3 秒，從背景切回來立刻同步
- [x] Mac 開發環境：setup-mac.sh 一鍵設定、DEVELOPMENT.md、金鑰外洩防呆
- [x] 不必離開 App 就能接上 Spotify：沒在播時給「接著播」，
      直接用 Web API 恢復播放；真的沒有裝置時才需要「打開 Spotify」
      （iOS 不允許 App 一啟動就把使用者踢去別的 App，那也是壞體驗）
- [x] 沒網路、Spotify 沒在播、逐字稿還沒好各有各的畫面與說法
      （離線時保留最後一集，跟暫停一樣不把逐字稿收走）
- [x] 減少匯入次數：逐字稿已經全走後端，App 只存「聽到哪」這種本機才有意義的東西。
      階段 4 的字典也要照這個原則走後端，不要綁進 App。
- [x] 斷句瑕疵已修（`segment.refine_word_boundaries` 用形態素解析重新切詞，
      所以斷句只會切在真正的詞邊界）。資料庫裡 09-05 產生的那幾份還看得到
      舊的切法，那是資料舊不是程式壞。實測 1,043 個句子接縫只有 0.9% 可疑，
      而且全部來自修正前的 Deepgram 資料。

---

## 已知限制

- **憑證 7 天到期**：免費 Apple 帳號的限制。到期要接電腦用 iloader 重裝。
  SideStore 的無線續簽目前壞掉（Apple 擋掉舊 User-Agent，AltSign PR #47 未合併），
  修好之後就能免接線。
- **Spotify 開發者模式**：限 5 個授權使用者且需 Premium。自己用沒問題，不能公開發布。
- **Spotify 獨家節目**：沒有公開 RSS，拿不到音檔，做不了逐字稿。
- **不能碰的東西**：Supabase 專案 `jglxgtumcbtquwuuqqep` 是公司專案。
